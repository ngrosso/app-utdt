import { InputFilterPipe } from './input-filter.pipe';

describe('InputFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InputFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
